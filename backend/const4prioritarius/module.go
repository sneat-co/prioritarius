// Package const4prioritarius holds this extension's platform-facing
// constants: its extension id and the Firestore collection names its keys
// build on. See models4prioritarius for the key builders that use them.
package const4prioritarius

// ExtensionID is this extension's canonical id — the value registered with
// the platform (space-module key, analytics event prefix, etc). customize.sh
// updates this value automatically; no manual edit needed after scaffolding.
const ExtensionID = "prioritarius"
